# Minimal reverse proxy / static hosting for goravel-admin.
#
# 1) API vhost  - REQUIRED pieces: /ws + /  -> Go :3000
# 2) Admin SPA  - OPTIONAL separate vhost when frontend is on the same server
#
# Typical API: SSL at Cloudflare, origin :80 only.
# Cloudflare: Network -> WebSockets On; SSL/TLS Flexible (or Full if origin has certs).
#
# Build frontend first:
#   cd html-react && npm ci && npm run build   # dist -> html-react/dist (recommended)
#   # or: cd html && npm ci && npm run build  # Vue dist -> html/dist
# Set SPA VITE_API_BASE_URL to your API origin (e.g. https://api.example.com).

upstream goravel_api {
  server 127.0.0.1:3000;
  keepalive 32;
}

# ---------------------------------------------------------------------------
# API (required)
# ---------------------------------------------------------------------------
server {
  listen 80;
  server_name api.example.com;

  client_max_body_size 10G;

  # REQUIRED: WebSocket /ws/admin/notifications
  location /ws {
    proxy_pass http://goravel_api;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400s;
    proxy_send_timeout 86400s;
    proxy_buffering off;
  }

  # REQUIRED: reverse proxy everything else to Go
  location / {
    proxy_pass http://goravel_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Connection "";
    proxy_connect_timeout 60s;
    proxy_read_timeout 86400s;
    proxy_send_timeout 300s;
  }

  # OPTIONAL: long-lived SSE (monitor system-info stream). Enable if SSE drops early.
  # location ~ ^/api/admin/monitor/system-info/stream {
  #   proxy_pass http://goravel_api;
  #   proxy_http_version 1.1;
  #   proxy_set_header Host $host;
  #   proxy_set_header X-Real-IP $remote_addr;
  #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
  #   proxy_set_header X-Forwarded-Proto $scheme;
  #   proxy_buffering off;
  #   proxy_cache off;
  #   proxy_read_timeout 86400s;
  #   proxy_send_timeout 86400s;
  #   gzip off;
  # }

  # OPTIONAL: serve local-disk attachments. Skip if FILESYSTEM_DISK=s3/r2 only.
  # location /storage/ {
  #   alias /path/to/storage/app/public/;
  #   expires 1h;
  #   add_header Cache-Control "public";
  #   access_log off;
  # }
}

# ---------------------------------------------------------------------------
# Admin SPA (optional) - enable when hosting frontend on this server
# Prefer a separate hostname (admin.example.com). Do not mix SPA root with API /.
# ---------------------------------------------------------------------------
# server {
#   listen 80;
#   server_name admin.example.com;
#
#   # React (recommended): html-react/dist
#   # Vue: html/dist
#   root /www/wwwroot/goravel/html-react/dist;
#   index index.html;
#
#   # Hashed assets - long cache
#   location /assets/ {
#     try_files $uri =404;
#     expires 1y;
#     add_header Cache-Control "public, immutable";
#     access_log off;
#   }
#
#   location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot|map)$ {
#     try_files $uri =404;
#     expires 1y;
#     add_header Cache-Control "public, immutable";
#     access_log off;
#   }
#
#   # Same-origin WSS for vanity hosts (SPA uses wss://this-host/ws/...).
#   # Only needed when this SPA vhost is not the API host.
#   # location /ws {
#   #   proxy_pass http://goravel_api;
#   #   proxy_http_version 1.1;
#   #   proxy_set_header Upgrade $http_upgrade;
#   #   proxy_set_header Connection "upgrade";
#   #   proxy_set_header Host $host;
#   #   proxy_set_header X-Real-IP $remote_addr;
#   #   proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#   #   proxy_set_header X-Forwarded-Proto $scheme;
#   #   proxy_read_timeout 86400s;
#   #   proxy_send_timeout 86400s;
#   #   proxy_buffering off;
#   # }
#
#   # SPA fallback (vue-router / react-router history mode)
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
#
#   gzip on;
#   gzip_min_length 1k;
#   gzip_comp_level 5;
#   gzip_types text/plain text/css application/javascript application/json image/svg+xml;
#   gzip_vary on;
# }

# OPTIONAL: terminate TLS on origin (Baota cert, no Cloudflare proxy).
# Duplicate the chosen server block(s); add listen 443 ssl and certificate paths.
# server {
#   listen 443 ssl http2;
#   server_name api.example.com;
#   ssl_certificate     /path/to/fullchain.pem;
#   ssl_certificate_key /path/to/privkey.pem;
#   # ... same locations as the :80 API (or SPA) server ...
# }
